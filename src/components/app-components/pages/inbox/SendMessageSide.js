// UI imports
import { Typography } from '@material-ui/core'


function SendMessageSide() {
    return (
        <div
            style={{ display: "flex", height: "100%", justifyContent: "center", alignItems: "center" }}>
            <Typography>Send a new Message</Typography>
        </div>)
}

export default SendMessageSide
